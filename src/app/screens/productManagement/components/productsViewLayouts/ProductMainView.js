import React, { memo, useState } from 'react';
import PropTypes from 'prop-types';

const ProductMainView = props => {
  return (
    <div className="productmanagement-container">
      <h1>Product List Screen</h1>
    </div>
  );
};

export default memo(ProductMainView);
