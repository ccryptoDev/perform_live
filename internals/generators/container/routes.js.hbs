/**
 *
 * {{ name }} Routes
 * Import the route file in app.routes to be processed by RoutesWrapper
 *
 */

import { lazy } from 'react';
import { IMG } from './{{ camelCase name }}.dependencies';

const {{ name }} = lazy(() => import('.'));
// Import child screen routes here

export default [
  {
    exact: true,
    path: '/{{ lowerCase name }}',
    screen: {{ name }},
    isPrivate: {{ isPrivate }},
    sidebar: {{ displayInSidebar }},
    name: '{{ name }}',
    icon: IMG.IC_DASHBOARD, // TODO: Change the Screen icon here
  },
];
